#include	<stdio.h>

/**
 * main - this is the function housing the code
 *
 * Return: if correct will return(0)
 *
 */

int	main(void)

{
	char c;
	
	for (c = 'a'; c <= 'z'; c++)
		putchar(c);
	
	putchar('\n');
	return (0);
}
